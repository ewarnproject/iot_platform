name: iot_platform
description: A new Flutter project for an IoT platform.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  provider: ^6.0.5
  shared_preferences: ^2.2.0
  url_launcher: ^6.1.12
  flutter_blue_plus: ^1.15.5
  network_info_plus: ^4.0.2
  ping_discover_network_forked: ^0.0.1
  flutter_libserialport: ^0.4.0
  github: ^9.2.0
  path_provider: ^2.1.1
  file_picker: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
